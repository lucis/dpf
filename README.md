<h1 align="center">
  Deus, Pátria e Família
</h1>

**Site em construção!**

## Como desenvolver

### Dependências
- `node` e `npm`/`yarn` instalados.
- `gatsby` instalado (`yarn global add gatsby`)

### Rodando

`yarn && yarn develop`
